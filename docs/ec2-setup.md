# EC2 setup & Linux basics checklist

- Generate/import SSH key
- Create a security group (SSH restricted to my IP; HTTP & 5000 for tests)
- Launch Ubuntu EC2 (t3.micro/t2.micro)
- SSH in and run Linux basics
- Install python3, venv, git; run minimal Flask smoke-test
- Push notes to git branch and open PR
